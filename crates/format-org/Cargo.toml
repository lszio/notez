[package]
name = "notez-format-org"
version = "0.1.0"
edition = "2024"
description = "Org format adapter for Notez"

[lib]
path = "src/lib.rs"

[dependencies]
notez-domain = { path = "../domain" }
notez-ports = { path = "../ports" }
