[package]
name = "notez-fs"
version = "0.1.0"
edition = "2024"
description = "Filesystem source and watcher infrastructure for Notez"

[lib]
path = "src/lib.rs"

[dependencies]
notez-domain = { path = "../domain" }
notez-ports = { path = "../ports" }
