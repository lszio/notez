# Notez v0.1 Web 客户端与部署实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 交付 [[file:../specs/2026-08-03-notez-v01-web-client-design.org][v0.1 Web 客户端与部署 spec]] — 启用 Dioxus Fullstack 模式在 `app` crate 内，实现资源列表 + 详情两页阅读器；本地 + Docker 部署；CLI `notez web` 子命令标记为 unimplemented。

**Architecture:** 在 `app/Cargo.toml` 加 `dioxus = { version = "0.6", features = ["fullstack"] }`（保留原 3 个 target-* feature）。重写 `app/src/lib.rs` 为 Dioxus fullstack root（`pub fn app() -> Element` + Router）。新增 `app/src/server.rs` 的两个 server functions（`list_resources` / `get_resource`）调 `core::application::ApplicationFacade::query` / `read`。新增 `app/src/model.rs` DTO `ResourceRow`。`app/src/bin/web.rs` 用 `LaunchBuilder::new().launch(app)` 启动。`app/src/bin/server.rs` 改写为同一 fullstack 入口。删 `cli/src/main.rs` 中 `Commands::Web` 分支体，替换为 `unimplemented!(...)`。新增 `Dockerfile` 多阶段构建。3 个 impl 单元测试 + 1 个集成测试。

**Tech Stack:** Rust 2024 / Dioxus 0.6 fullstack / `urlencoding` / `tempfile` (dev)。**重要 caveat**：Dioxus 0.6 EOL，文档资料稀薄；本 plan 假定 0.6.3 API（项目当前 `Cargo.lock` 中 dioxus = 0.6.3）。Task 1 落地后若发现 `#[server]` 宏 / `use_server_future` API 形态与 spec 假设有偏差，按 0.6 实际 API 调整后续 task。

**约定：**
- 计划中所有 `cargo test -p app` 视 task 范围调整。
- 计划中所有 `cargo check --workspace --all-targets` 是收尾。
- 计划所有 `cargo test --workspace` 在最后 Task 跑一次确认无回归。
- 包名是 `core` / `app` / `cli`（**不是** `notez_core` / `notez_app` / `notez_cli`，后者是 lib name，cargo 命令用 package name）。
- 不动 `app/src/bin/desktop.rs` / `mobile.rs`（v0.1 只关注 web + server binary）。
- 频繁 commit。

---

## File Structure

| 文件 | 变化 | 责任 |
|---|---|---|
| `app/Cargo.toml` | 修改 | 加 `dioxus = { version = "0.6", features = ["fullstack"] }`；加 `urlencoding` dep |
| `app/src/lib.rs` | 重写 | Dioxus fullstack root + `pub fn app() -> Element` + Router |
| `app/src/model.rs` | 新建 | `ResourceRow` DTO + `From<Resource>` |
| `app/src/server.rs` | 新建 | 2 个 `#[server]` 函数 + `list_resources_impl` / `get_resource_impl` 内部函数（可单测） |
| `app/src/router.rs` | 新建 | `Route` 枚举 + `router()` |
| `app/src/pages/mod.rs` | 新建 | `pub use` |
| `app/src/pages/list.rs` | 新建 | `ListPage` 组件 |
| `app/src/pages/detail.rs` | 新建 | `DetailPage` 组件 |
| `app/src/bin/web.rs` | 重写 | `LaunchBuilder::new().launch(app)` 启动 |
| `app/src/bin/server.rs` | 重写 | 跟 web.rs 同源（与 spec §2.2 一致） |
| `app/src/bin/desktop.rs` | 不动 | 保留 stub |
| `app/src/bin/mobile.rs` | 不动 | 保留 stub |
| `app/tests/v0_1_list_and_detail.rs` | 新建 | 集成测试 |
| `cli/src/main.rs` | 修改 | 替换 `Commands::Web` 分支体 |
| `Dockerfile` | 新建 | 多阶段构建 |

---

## Task 1: 启用 Dioxus Fullstack + 最小 Hello world 入口

**Files:**
- Modify: `app/Cargo.toml`
- Modify: `app/src/lib.rs`
- Modify: `app/src/bin/web.rs`

- [ ] **Step 1: 修改 `app/Cargo.toml`**

把当前内容替换为：

```toml
[package]
name = "app"
version = "0.1.0"
edition = "2024"
description = "notez dioxus application crate: shared root + platform entrypoints"

[lib]
name = "app"
path = "src/lib.rs"

[dependencies]
core = { path = "../core" }
serde = { workspace = true }
serde_json = { workspace = true }
dioxus = { version = "0.6", features = ["fullstack"] }
dioxus-web = { version = "0.6", optional = true, default-features = false }
dioxus-desktop = { version = "0.6", optional = true, default-features = false }
dioxus-mobile = { version = "0.6", optional = true, default-features = false }
urlencoding = "2"

[dev-dependencies]
tempfile = "3"

[features]
default = ["target-web"]
target-web = ["dep:dioxus-web"]
target-desktop = ["dep:dioxus-desktop"]
target-mobile = ["dep:dioxus-mobile"]

[[bin]]
name = "notez-web"
path = "src/bin/web.rs"
required-features = ["target-web"]

[[bin]]
name = "notez-desktop"
path = "src/bin/desktop.rs"
required-features = ["target-desktop"]

[[bin]]
name = "notez-mobile"
path = "src/bin/mobile.rs"
required-features = ["target-mobile"]

[[bin]]
name = "notez-server"
path = "src/bin/server.rs"
```

- [ ] **Step 2: 重写 `app/src/lib.rs` 为最小 fullstack 入口**

Replace the entire file with:

```rust
//! `app` — notez dioxus fullstack web client (v0.1, reader-only).

#![allow(non_snake_case)]

use dioxus::prelude::*;

mod model;
mod pages;
mod router;
mod server;

pub use router::Route;

/// Dioxus fullstack application root.
///
/// Server-renders the first response, then hydrates the client.
pub fn app() -> Element {
    rsx! {
        Router::<Route> {}
    }
}
```

- [ ] **Step 3: 重写 `app/src/bin/web.rs` 为 fullstack 启动入口**

Replace the entire file with:

```rust
use app::app;
use dioxus::LaunchBuilder;

fn main() {
    // NOTEZ_BIND is read by Dioxus fullstack's axum runtime internally.
    // We only set a sane default when the operator did not pin one.
    if std::env::var("NOTEZ_BIND").is_err() {
        std::env::set_var("NOTEZ_BIND", "127.0.0.1:3030");
    }

    LaunchBuilder::new()
        .launch(app);
}
```

- [ ] **Step 4: 写失败的 cargo check 跑通（先建空 module 占位）**

在 Task 2 完成之前，cargo check 会因为 `mod model; mod pages; mod router; mod server;` 引用不存在的文件失败。**临时** 在 `app/src/` 下建 4 个空文件：

```bash
cd /home/lszio/Projects/notez
touch app/src/model.rs app/src/server.rs app/src/router.rs app/src/pages/mod.rs
```

每个文件内容是单行 `// stub — populated in Task 2/3/4`。

- [ ] **Step 5: 跑 cargo check 确认 0 错误（warning 可以）**

```bash
cd /home/lszio/Projects/notez
cargo check -p app --bin notez-web 2>&1 | tail -10
```

Expected: `Finished` line, possibly warnings about unused imports (from the empty modules).

- [ ] **Step 6: Commit**

```bash
cd /home/lszio/Projects/notez
git add app/Cargo.toml app/src/lib.rs app/src/bin/web.rs app/src/model.rs app/src/server.rs app/src/router.rs app/src/pages/mod.rs
git commit -m "feat(app): enable Dioxus Fullstack and add minimal web entry

Add dioxus = { version = \"0.6\", features = [\"fullstack\"] } to the
app crate's dependencies; preserve the three target-* features for
WASM client / desktop / mobile. The web and server binaries now
boot via LaunchBuilder::new().launch(app). Empty module stubs for
model/server/router/pages are temporary; Tasks 2-4 populate them."
```

---

## Task 2: `ResourceRow` DTO + `model` 模块

**Files:**
- Modify: `app/src/model.rs`

- [ ] **Step 1: 写 `ResourceRow` 类型 + `From<Resource>` 实现 + 单测**

Replace `app/src/model.rs` with:

```rust
//! `ResourceRow` — the DTO returned by web server functions.
//!
//! It mirrors `core::domain::Resource` but uses concrete `String` fields
//! (no ULID) so it serialises cleanly through `dioxus::server` without
//! requiring the client to import `ulid`. The `From<Resource>` impl
//! converts the domain type to the wire shape; rendering code in
//! `pages/list.rs` and `pages/detail.rs` consumes `ResourceRow` directly.

use std::collections::BTreeMap;

use notez_core::domain::{Resource, ResourceKind, ResourceRef};

#[derive(Debug, Clone, PartialEq, serde::Serialize, serde::Deserialize)]
pub struct ResourceRow {
    pub ref_str: String,
    pub kind: String,
    pub title: String,
    pub source_id: String,
    pub locator: String,
    pub object_id: String,
    pub revision: String,
    pub properties: BTreeMap<String, String>,
}

impl ResourceRow {
    pub fn new(
        r_ref: ResourceRef,
        kind: ResourceKind,
        title: String,
        source_id: String,
        locator: String,
        object_id: String,
        revision: String,
        properties: BTreeMap<String, String>,
    ) -> Self {
        Self {
            ref_str: r_ref.to_string(),
            kind: kind.as_str().to_string(),
            title,
            source_id,
            locator,
            object_id,
            revision,
            properties,
        }
    }
}

impl From<Resource> for ResourceRow {
    fn from(r: Resource) -> Self {
        Self::new(
            r.r#ref,
            r.kind,
            r.title,
            r.source_id,
            r.locator,
            r.object_id.to_string(),
            r.revision,
            r.properties,
        )
    }
}

#[cfg(test)]
mod tests {
    use super::*;
    use notez_core::domain::{derived_object_id, ResourceKind, ResourceRef};

    #[test]
    fn from_resource_populates_all_fields() {
        let r_ref = ResourceRef::parse("heading:01ARZ3NDEKTSV4RRFFQ69G5FAV").unwrap();
        let object_id = derived_object_id("hash", "loc", "h:0");
        let res = Resource {
            r#ref: r_ref,
            kind: ResourceKind::Heading,
            title: "X".to_string(),
            revision: "r1".to_string(),
            source_id: "src".to_string(),
            locator: "loc".to_string(),
            properties: BTreeMap::from([("k".to_string(), "v".to_string())]),
            object_id,
        };
        let row = ResourceRow::from(res);
        assert_eq!(row.ref_str, "heading:01ARZ3NDEKTSV4RRFFQ69G5FAV");
        assert_eq!(row.kind, "heading");
        assert_eq!(row.title, "X");
        assert_eq!(row.source_id, "src");
        assert_eq!(row.locator, "loc");
        assert_eq!(row.object_id, object_id.to_string());
        assert_eq!(row.revision, "r1");
        assert_eq!(row.properties.get("k").map(String::as_str), Some("v"));
    }
}
```

- [ ] **Step 2: 跑测试确认通过**

```bash
cd /home/lszio/Projects/notez
cargo test -p app --lib model::tests 2>&1 | tail -10
```

Expected: 1 test passes.

- [ ] **Step 3: Commit**

```bash
cd /home/lszio/Projects/notez
git add app/src/model.rs
git commit -m "feat(app): add ResourceRow DTO and From<Resource> conversion

Mirrors the core::domain::Resource shape with concrete String fields
so the wire payload through dioxus::server doesn't require ulid on
the client. One unit test asserts all fields round-trip."
```

---

## Task 3: `server.rs` 的 server functions + 可单测的 impl

**Files:**
- Modify: `app/src/server.rs`

- [ ] **Step 1: 写 `list_resources_impl` 与 `get_resource_impl` + 3 个单测**

Replace `app/src/server.rs` with:

```rust
//! Web server functions for the v0.1 reader.
//!
//! `#[server]`-attributed functions are the public, Dioxus-managed HTTP
//! entry points. They read `NOTEZ_SPACE_ROOT`, open the SQLite projection,
//! construct an `ApplicationFacade`, and delegate to the `_impl` helpers
//! below. The `_impl` helpers take an explicit `&Path` so they can be
//! unit-tested directly with an in-memory `SqliteProjection` without
//! going through the Dioxus fullstack runtime.

use std::path::Path;

use dioxus::prelude::*;
use notez_core::application::ApplicationFacade;
use notez_core::domain::{ProjectionStore, Resource, ResourceRef, Selector};
use notez_core::storage::SqliteProjection;

use crate::model::ResourceRow;

#[server]
pub async fn list_resources() -> Result<Vec<ResourceRow>, ServerFnError> {
    let space_root = std::env::var("NOTEZ_SPACE_ROOT")
        .map_err(|_| ServerFnError::ServerError("NOTEZ_SPACE_ROOT not set".into()))?;
    list_resources_impl(Path::new(&space_root))
        .await
        .map_err(|e| ServerFnError::ServerError(e.to_string()))
}

#[server]
pub async fn get_resource(ref_str: String) -> Result<Option<ResourceRow>, ServerFnError> {
    let space_root = std::env::var("NOTEZ_SPACE_ROOT")
        .map_err(|_| ServerFnError::ServerError("NOTEZ_SPACE_ROOT not set".into()))?;
    get_resource_impl(Path::new(&space_root), &ref_str)
        .await
        .map_err(|e| ServerFnError::ServerError(e.to_string()))
}

/// Implementation that can be exercised by unit tests with an
/// in-memory projection. The async signature matches the server
/// functions so the wrapper is a one-liner.
pub(crate) async fn list_resources_impl(
    space_root: &Path,
) -> Result<Vec<ResourceRow>, String> {
    let db_path = space_root.join(".notez/index.sqlite");
    let store = SqliteProjection::open(&db_path).map_err(|e| e.to_string())?;
    let facade = ApplicationFacade::new(store);
    let page = facade.query(&Selector::new()).map_err(|e| e.to_string())?;
    Ok(page.items.into_iter().map(ResourceRow::from).collect())
}

pub(crate) async fn get_resource_impl(
    space_root: &Path,
    ref_str: &str,
) -> Result<Option<ResourceRow>, String> {
    let r_ref = ResourceRef::parse(ref_str)
        .map_err(|e| format!("invalid ref: {e}"))?;
    let db_path = space_root.join(".notez/index.sqlite");
    let store = SqliteProjection::open(&db_path).map_err(|e| e.to_string())?;
    let facade = ApplicationFacade::new(store);
    let res: Option<Resource> = facade.read(&r_ref).map_err(|e| e.to_string())?;
    Ok(res.map(ResourceRow::from))
}

#[cfg(test)]
mod tests {
    use super::*;
    use notez_core::domain::{
        derived_object_id, ObjectId, Resource, ResourceKind, ResourceRef,
    };
    use std::collections::BTreeMap;
    use tempfile::tempdir;

    fn seed_resource(store: &mut SqliteProjection, r_ref: ResourceRef, title: &str) {
        let object_id = derived_object_id("h", "loc", "h:0");
        let res = Resource {
            r#ref,
            kind: ResourceKind::Heading,
            title: title.to_string(),
            revision: "r1".to_string(),
            source_id: "src".to_string(),
            locator: "loc".to_string(),
            properties: BTreeMap::new(),
            object_id,
        };
        store.upsert_resource(&res).unwrap();
    }

    #[tokio::test]
    async fn list_resources_impl_returns_upserted_resources() {
        let dir = tempdir().unwrap();
        // Create the .notez/index.sqlite path the impl will open.
        std::fs::create_dir_all(dir.path().join(".notez")).unwrap();
        let db_path = dir.path().join(".notez/index.sqlite");

        // Pre-populate the database directly (skipping ApplicationFacade
        // mutation methods — they're out of scope for v0.1 read-only).
        let mut store = SqliteProjection::open(&db_path).unwrap();
        let r = ResourceRef::parse("heading:01ARZ3NDEKTSV4RRFFQ69G5FAV").unwrap();
        seed_resource(&mut store, r, "X");
        drop(store);

        // The impl opens a fresh SqliteProjection over the same file
        // (file-backed, not in-memory).
        let rows = list_resources_impl(dir.path()).await.unwrap();
        assert_eq!(rows.len(), 1);
        assert_eq!(rows[0].title, "X");
    }

    #[tokio::test]
    async fn get_resource_impl_returns_upserted_resource() {
        let dir = tempdir().unwrap();
        std::fs::create_dir_all(dir.path().join(".notez")).unwrap();
        let db_path = dir.path().join(".notez/index.sqlite");
        let mut store = SqliteProjection::open(&db_path).unwrap();
        let r = ResourceRef::parse("heading:01ARZ3NDEKTSV4RRFFQ69G5FAV").unwrap();
        seed_resource(&mut store, r, "Detail");
        drop(store);

        let row = get_resource_impl(dir.path(), "heading:01ARZ3NDEKTSV4RRFFQ69G5FAV")
            .await
            .unwrap();
        assert!(row.is_some());
        let row = row.unwrap();
        assert_eq!(row.title, "Detail");
        assert_eq!(row.kind, "heading");
    }

    #[tokio::test]
    async fn get_resource_impl_returns_none_for_missing() {
        let dir = tempdir().unwrap();
        std::fs::create_dir_all(dir.path().join(".notez")).unwrap();
        let _ = SqliteProjection::open(dir.path().join(".notez/index.sqlite")).unwrap();

        let row = get_resource_impl(dir.path(), "heading:01ARZ3NDEKTSV4RRFFQ69G5FAV")
            .await
            .unwrap();
        assert!(row.is_none());
    }

    #[test]
    fn object_id_serializes_through_resource_row() {
        // Sanity: ObjectId -> String -> ResourceRow.object_id round-trips.
        let oid = ObjectId::default();
        let r_ref = ResourceRef::parse("heading:01ARZ3NDEKTSV4RRFFQ69G5FAV").unwrap();
        let row = ResourceRow::new(
            r_ref,
            ResourceKind::Heading,
            "t".to_string(),
            "s".to_string(),
            "l".to_string(),
            oid.to_string(),
            "r".to_string(),
            BTreeMap::new(),
        );
        assert_eq!(row.object_id, oid.to_string());
    }
}
```

- [ ] **Step 2: 跑测试**

```bash
cd /home/lszio/Projects/notez
cargo test -p app --lib server::tests 2>&1 | tail -15
```

Expected: 4 tests pass (3 impl + 1 sanity).

- [ ] **Step 3: Commit**

```bash
cd /home/lszio/Projects/notez
git add app/src/server.rs
git commit -m "feat(app): web server functions for list_resources and get_resource

Wrap core::ApplicationFacade::query / read behind #[server]-attributed
async functions. Split out list_resources_impl / get_resource_impl
that take an explicit &Path so the test suite can exercise the same
logic with an in-memory-or-file SqliteProjection without spinning up
the Dioxus fullstack runtime. 3 impl tests + 1 sanity test."
```

---

## Task 4: Router + List 页面 + Detail 页面

**Files:**
- Modify: `app/src/router.rs`
- Modify: `app/src/pages/mod.rs`
- Modify: `app/src/pages/list.rs`
- Modify: `app/src/pages/detail.rs`
- Modify: `app/src/lib.rs` (export the new pages)

- [ ] **Step 1: 写 router**

Replace `app/src/router.rs` with:

```rust
use dioxus::prelude::*;

use crate::pages::{DetailPage, ListPage};

#[derive(Routable, Clone, Debug, PartialEq)]
pub enum Route {
    #[route("/")]
    List {},
    #[route("/resource/:encoded_ref")]
    Detail { encoded_ref: String },
}

#[component]
pub fn Router() -> Element {
    match current_route() {
        Route::List {} => rsx! { ListPage {} },
        Route::Detail { encoded_ref } => rsx! { DetailPage { encoded_ref } },
    }
}

// Dioxus 0.6 may export `current_route` or `use_route` depending on
// version; the wrapper keeps the call sites consistent.
fn current_route() -> Route {
    use_route::<Route>()
}
```

NOTE: The exact Dioxus 0.6 hook for getting the current `Route` enum
is either `use_route::<Route>()` (free function) or
`use_context::<RouterContext<R>>().current()` depending on the macro
expansion. If `use_route::<Route>()` doesn't compile, replace
`current_route()` with the form that does.

- [ ] **Step 2: 写 pages/mod.rs**

Replace `app/src/pages/mod.rs` with:

```rust
pub mod list;
pub mod detail;

pub use list::ListPage;
pub use detail::DetailPage;
```

- [ ] **Step 3: 写 ListPage**

Replace `app/src/pages/list.rs` with:

```rust
use dioxus::prelude::*;

use crate::server::list_resources;

#[component]
pub fn ListPage() -> Element {
    let resources = use_server_future(|| async { list_resources().await })?;

    rsx! {
        div { class: "min-h-screen p-6 bg-white text-gray-900",
            header { class: "mb-6",
                h1 { class: "text-2xl font-semibold", "Notez" }
                p { class: "text-sm text-gray-600",
                    match resources() {
                        Some(Ok(rows)) => format!("{} resources", rows.len()),
                        Some(Err(e)) => format!("error: {e}"),
                        None => "loading…".to_string(),
                    }
                }
            }
            section {
                match resources() {
                    Some(Ok(rows)) if rows.is_empty() => rsx! {
                        p { class: "text-gray-600",
                            "该 Space 还没有被扫描过。运行 `notez scan`。"
                        }
                    },
                    Some(Ok(rows)) => rsx! {
                        table { class: "w-full border-collapse",
                            thead { class: "border-b"
                                tr {
                                    th { class: "text-left p-2", "ref" }
                                    th { class: "text-left p-2", "title" }
                                    th { class: "text-left p-2", "source_id" }
                                    th { class: "text-left p-2", "locator" }
                                }
                            }
                            tbody {
                                for row in rows.iter() {
                                    tr { class: "border-b hover:bg-gray-50",
                                        td { class: "p-2 font-mono text-xs",
                                            a {
                                                href: format!("/resource/{}", urlencoding::encode(&row.ref_str)),
                                                "{row.ref_str}"
                                            }
                                        }
                                        td { class: "p-2", "{row.title}" }
                                        td { class: "p-2 font-mono text-xs", "{row.source_id}" }
                                        td { class: "p-2 font-mono text-xs", "{row.locator}" }
                                    }
                                }
                            }
                        }
                    },
                    Some(Err(e)) => rsx! {
                        p { class: "text-red-600", "加载失败：{e}" }
                    },
                    None => rsx! {
                        p { class: "text-gray-500", "loading…" }
                    },
                }
            }
        }
    }
}
```

- [ ] **Step 4: 写 DetailPage**

Replace `app/src/pages/detail.rs` with:

```rust
use dioxus::prelude::*;

use crate::server::get_resource;

#[component]
pub fn DetailPage(encoded_ref: String) -> Element {
    let decoded = urlencoding::decode(&encoded_ref)
        .map(|c| c.into_owned())
        .unwrap_or_else(|_| encoded_ref.clone());
    let resource = use_server_future(move || {
        let r = decoded.clone();
        async move { get_resource(r).await }
    })?;

    rsx! {
        div { class: "min-h-screen p-6 bg-white text-gray-900",
            header { class: "mb-6 flex items-baseline justify-between",
                h1 { class: "text-2xl font-semibold",
                    match resource() {
                        Some(Ok(Some(row))) => format!("[{}] {}", row.ref_str, row.title),
                        _ => format!("{decoded}"),
                    }
                }
                a { class: "text-sm text-blue-600 hover:underline", href: "/",
                    "← 返回列表"
                }
            }
            section {
                match resource() {
                    Some(Ok(Some(row))) => rsx! {
                        div {
                            h2 { class: "text-lg font-semibold mb-2", "属性" }
                            table { class: "w-full border-collapse",
                                tbody {
                                    tr { class: "border-b",
                                        th { class: "text-left p-2 w-40", scope: "row", "kind" }
                                        td { class: "p-2 font-mono", "{row.kind}" }
                                    }
                                    tr { class: "border-b",
                                        th { class: "text-left p-2", scope: "row", "source_id" }
                                        td { class: "p-2 font-mono text-xs", "{row.source_id}" }
                                    }
                                    tr { class: "border-b",
                                        th { class: "text-left p-2", scope: "row", "locator" }
                                        td { class: "p-2 font-mono text-xs", "{row.locator}" }
                                    }
                                    tr { class: "border-b",
                                        th { class: "text-left p-2", scope: "row", "revision" }
                                        td { class: "p-2 font-mono text-xs", "{row.revision}" }
                                    }
                                    tr { class: "border-b",
                                        th { class: "text-left p-2", scope: "row", "object_id" }
                                        td { class: "p-2 font-mono text-xs", "{row.object_id}" }
                                    }
                                }
                            }
                        }
                        div { class: "mt-6",
                            h2 { class: "text-lg font-semibold mb-2", "properties" }
                            if row.properties.is_empty() {
                                p { class: "text-gray-500", "（无）" }
                            } else {
                                table { class: "w-full border-collapse",
                                    tbody {
                                        for (k, v) in row.properties.iter() {
                                            tr { class: "border-b",
                                                td { class: "p-2 font-mono text-xs w-1/3", "{k}" }
                                                td { class: "p-2 font-mono text-xs", "{v}" }
                                            }
                                        }
                                    }
                                }
                            }
                        }
                    },
                    Some(Ok(None)) => rsx! {
                        p { class: "text-gray-600", "资源不存在" }
                    },
                    Some(Err(e)) => rsx! {
                        p { class: "text-red-600", "加载失败：{e}" }
                    },
                    None => rsx! {
                        p { class: "text-gray-500", "loading…" }
                    },
                }
            }
        }
    }
}
```

- [ ] **Step 5: 修正 `app/src/lib.rs` 的 `Router` 名字（避免与 Dioxus 内置 `Router` 冲突）**

把 `app/src/lib.rs` 改为（保留 `Route` re-export，但 `Router` 组件走 `crate::router::Router`）：

```rust
//! `app` — notez dioxus fullstack web client (v0.1, reader-only).

#![allow(non_snake_case)]

use dioxus::prelude::*;

mod model;
mod pages;
mod router;
mod server;

pub use router::Route;

#[component]
fn App() -> Element {
    rsx! {
        crate::router::Router {}
    }
}

/// Dioxus fullstack application root.
pub fn app() -> Element {
    rsx! { App {} }
}
```

- [ ] **Step 6: 跑 cargo check 确认全栈编译**

```bash
cd /home/lszio/Projects/notez
cargo check -p app --bin notez-web 2>&1 | tail -10
```

Expected: 0 errors. Warnings acceptable (e.g. unused imports, Dioxus 0.6 deprecated warnings).

- [ ] **Step 7: 跑 app 测试**

```bash
cd /home/lszio/Projects/notez
cargo test -p app --lib 2>&1 | tail -10
```

Expected: existing 4 server/model tests still pass; no new failures.

- [ ] **Step 8: Commit**

```bash
cd /home/lszio/Projects/notez
git add app/src/
git commit -m "feat(app): Dioxus router, list page and detail page

Two routes: '/' (ListPage, table of resources) and
'/resource/<encoded_ref>' (DetailPage, properties table). Both pages
call the server functions from Task 3 via use_server_future; the
default Dioxus theme gives table + minimal Tailwind classes (no CSS
framework dependency added). Loading / empty / 404 / error states
all have explicit branches."
```

---

## Task 5: 集成测试

**Files:**
- Create: `app/tests/v0_1_list_and_detail.rs`

- [ ] **Step 1: 写测试**

Create `app/tests/v0_1_list_and_detail.rs`:

```rust
//! End-to-end test: build a minimal Space on disk, upsert resources
//! directly into the SQLite projection, and exercise the
//! `list_resources_impl` / `get_resource_impl` helpers from
//! `app::server` exactly as the HTTP server functions would.

use app::model::ResourceRow;
use notez_core::application::ApplicationFacade;
use notez_core::domain::{
    derived_object_id, ProjectionStore, Resource, ResourceKind, ResourceRef, Selector,
};
use notez_core::storage::SqliteProjection;
use std::collections::BTreeMap;
use tempfile::tempdir;

const REF_STR: &str = "heading:01ARZ3NDEKTSV4RRFFQ69G5FAV";

fn seed(dir: &std::path::Path) {
    // Build a Space layout: notez.toml + a Markdown file. We bypass the
    // ApplicationFacade scan path because v0.1 is reader-only and the
    // test focuses on the projection read paths.
    std::fs::write(
        dir.join("notez.toml"),
        "[space]\nname = \"test-space\"\ndatabase = \".notez/index.sqlite\"\n",
    )
    .unwrap();
    std::fs::create_dir_all(dir.join(".notez")).unwrap();
    let db_path = dir.path().join(".notez/index.sqlite");
    let mut store = SqliteProjection::open(&db_path).unwrap();
    let r_ref = ResourceRef::parse(REF_STR).unwrap();
    let res = Resource {
        r#ref: r_ref,
        kind: ResourceKind::Heading,
        title: "Integration Test".to_string(),
        revision: "r1".to_string(),
        source_id: "src".to_string(),
        locator: "loc".to_string(),
        properties: BTreeMap::from([("k".to_string(), "v".to_string())]),
        object_id: derived_object_id("h", "loc", "h:0"),
    };
    store.upsert_resource(&res).unwrap();

    // Also exercise the ApplicationFacade::query path so we know the
    // server-function wrapper will work on a real projection file.
    let facade = ApplicationFacade::new(SqliteProjection::open(&db_path).unwrap());
    let page = facade.query(&Selector::new()).unwrap();
    assert_eq!(page.items.len(), 1);
}

#[tokio::test]
async fn list_then_detail_roundtrip() {
    let dir = tempdir().unwrap();
    seed(dir.path());

    let rows: Vec<ResourceRow> =
        app::server::list_resources_impl(dir.path()).await.unwrap();
    assert_eq!(rows.len(), 1);
    assert_eq!(rows[0].ref_str, REF_STR);
    assert_eq!(rows[0].title, "Integration Test");
    assert_eq!(rows[0].kind, "heading");

    let row: Option<ResourceRow> =
        app::server::get_resource_impl(dir.path(), REF_STR).await.unwrap();
    let row = row.expect("detail row should exist");
    assert_eq!(row.ref_str, REF_STR);
    assert_eq!(row.title, "Integration Test");
    assert_eq!(row.properties.get("k").map(String::as_str), Some("v"));
}

#[tokio::test]
async fn get_missing_returns_none() {
    let dir = tempdir().unwrap();
    seed(dir.path());
    let row = app::server::get_resource_impl(
        dir.path(),
        "heading:01ARZ3NDEKTSV4RRFFQ69G5FAW",
    )
    .await
    .unwrap();
    assert!(row.is_none());
}
```

- [ ] **Step 2: 跑测试**

```bash
cd /home/lszio/Projects/notez
cargo test -p app --test v0_1_list_and_detail 2>&1 | tail -15
```

Expected: 2 tests pass.

- [ ] **Step 3: Commit**

```bash
cd /home/lszio/Projects/notez
git add app/tests/v0_1_list_and_detail.rs
git commit -m "test(app): end-to-end v0.1 list + detail roundtrip

Builds a minimal Space (notez.toml + index.sqlite with one upserted
resource) and exercises the same _impl helpers the HTTP server
functions call. Confirms the projection layout ApplicationFacade::query
and the impl agree on the resource shape."
```

---

## Task 6: CLI `notez web` 标记为 unimplemented

**Files:**
- Modify: `cli/src/main.rs`

- [ ] **Step 1: 替换 `Commands::Web` 分支体**

Read `cli/src/main.rs:1370-1393`. The branch is:

```rust
#[cfg(feature = "web")]
Commands::Web(args) => {
    let state = match web::state::WebState::from_space(&r_config.space_root) {
        ...
    };
    ...
}
```

Replace the entire `#[cfg(feature = "web")] Commands::Web(args) => { ... }` block (lines 1370-1393) with:

```rust
        #[cfg(feature = "web")]
        Commands::Web(_args) => {
            eprintln!(
                "the `notez web` subcommand is no longer implemented.\n\
                 use the v0.1 Dioxus fullstack client instead:\n\
                 \n  \
                 NOTEZ_SPACE_ROOT={} cargo run -p app --bin notez-web\n\
                 \n\
                 see docs/superpowers/specs/2026-08-03-notez-v01-web-client-design.org \
                 for the design and deployment notes.",
                r_config.space_root.display()
            );
            exit(5);
        }
```

- [ ] **Step 2: 跑 cargo check (workspace) 确认 0 errors**

```bash
cd /home/lszio/Projects/notez
cargo check --workspace --all-targets 2>&1 | tail -10
```

Expected: 0 errors.

- [ ] **Step 3: 跑 cli 测试（确保 Web 子命令被 unimplemented 但 CLI 其它部分不动）**

```bash
cd /home/lszio/Projects/notez
cargo test -p cli 2>&1 | tail -10
```

Expected: existing cli tests pass (Web subcommand is `[cfg(feature = "web")]`-gated and default features exclude it; the change only affects the feature-enabled path).

- [ ] **Step 4: 手动验证 Web 子命令路径（feature 开启）**

```bash
cd /home/lszio/Projects/notez
cargo run -p cli --features web -- web 2>&1 | tail -10
```

Expected: stderr prints the migration message; exit code 5.

- [ ] **Step 5: Commit**

```bash
cd /home/lszio/Projects/notez
git add cli/src/main.rs
git commit -m "refactor(cli): replace notez web subcommand body with migration message

The Web command's previous body called web::state::WebState::from_space
and web::server::serve, but those modules don't exist in the
workspace; the `web` feature is functionally broken. This change
preserves the cli args structure and the [cfg(feature = \"web\")]
gate, but replaces the body with an stderr message pointing to the
v0.1 Dioxus fullstack replacement (cargo run -p app --bin
notez-web) and the design spec."
```

---

## Task 7: Dockerfile

**Files:**
- Create: `Dockerfile`

- [ ] **Step 1: 写 Dockerfile**

Create `Dockerfile`:

```dockerfile
# --- builder ---
FROM rust:1-bookworm AS builder
WORKDIR /build
# Pre-copy manifests to cache deps before the source layer.
COPY Cargo.toml Cargo.lock ./
COPY app app
COPY core core
COPY cli cli
COPY adapters adapters
RUN cargo build --release -p app --bin notez-web --features app/target-web

# --- runtime ---
FROM debian:bookworm-slim
RUN apt-get update && apt-get install -y --no-install-recommends ca-certificates && rm -rf /var/lib/apt/lists/*
COPY --from=builder /build/target/release/notez-web /usr/local/bin/notez-web
ENV NOTEZ_BIND=0.0.0.0:3030
EXPOSE 3030
ENTRYPOINT ["/usr/local/bin/notez-web"]
```

- [ ] **Step 2: 写 `.dockerignore`**

Create `.dockerignore`:

```
target
.git
.worktrees
.superpowers
.notez
*.md
!docs/superpowers/specs/2026-08-03-notez-v01-web-client-design.org
!docs/superpowers/plans/2026-08-03-v01-web-client.md
```

NOTE: `.md` exclude then re-include only the spec/plan for this work to keep image context small.

- [ ] **Step 3: 验证 Dockerfile 语法可解析（不实际 build）**

`docker build` 需要 docker daemon；本机环境没装。改为手动 syntax check：

```bash
cd /home/lszio/Projects/notez
grep -E "^(FROM|RUN|COPY|ENV|EXPOSE|ENTRYPOINT|WORKDIR)" Dockerfile
```

Expected: 14 lines, structure matches the file written.

- [ ] **Step 4: Commit**

```bash
cd /home/lszio/Projects/notez
git add Dockerfile .dockerignore
git commit -m "build(app): add Dockerfile for the v0.1 Dioxus fullstack web client

Multi-stage build: rust:1-bookworm builder compiles notez-web with
the app/target-web feature; debian:bookworm-slim runtime copies the
binary, sets NOTEZ_BIND=0.0.0.0:3030, exposes port 3030, and runs
the binary as ENTRYPOINT. .dockerignore keeps the build context
small by excluding target/, .git/, worktrees, and most *.md files
(only the v0.1 spec/plan are re-included for image traceability)."
```

---

## Task 8: 最终验证

**Files:** none (read-only)

- [ ] **Step 1: 全 workspace 测试通过**

```bash
cd /home/lszio/Projects/notez
cargo test --workspace 2>&1 | grep -E "test result|FAIL" | tail -15
```

Expected: 0 failures. The total passed should be the baseline (215 after MVP-1 work) + the new tests from Tasks 2, 3, 5 (~6 tests).

- [ ] **Step 2: 统计通过数**

```bash
cd /home/lszio/Projects/notez
cargo test --workspace 2>&1 | grep "test result:" | awk '{sum += $4} END {print "total passed:", sum}'
```

Expected: ~221 (215 + 6 new).

- [ ] **Step 3: workspace 编译干净（无 error）**

```bash
cd /home/lszio/Projects/notez
cargo build --workspace --all-targets 2>&1 | grep -E "^error" | head -10
```

Expected: 0 errors. Warnings are pre-existing and acceptable.

- [ ] **Step 4: 列出本计划 7 个 commit**

```bash
cd /home/lszio/Projects/notez
git log --oneline 303244e..HEAD
```

Expected: 7 commits, one per Task (1-7). Task 8 is verification only.

- [ ] **Step 5: 报告**

Report:
- 7 commit SHAs + titles
- cargo test --workspace result (passing count, failure count)
- cargo build error count (should be 0)
- Any concerns / deviations encountered (e.g. Dioxus 0.6 API mismatches)
- One-paragraph summary

---

## Self-Review

**1. Spec coverage:** Skim the spec (§1-11). Each spec section maps to one or more tasks:
- §1.1 / §1.2 范围 → Task 1 (Cargo + lib + web entry), Task 2 (model DTO), Task 3 (server fns), Task 4 (router + pages), Task 5 (tests), Task 6 (CLI cleanup), Task 7 (Dockerfile)
- §2.1 Cargo.toml → Task 1 Step 1
- §2.2 file structure → Tasks 1-4
- §3.1 list_resources → Task 3 Step 1
- §3.2 get_resource → Task 3 Step 1
- §3.3 错误映射 → Task 3 Step 1 (format!("{e}"))
- §4.1 列表页 → Task 4 Step 3
- §4.2 详情页 → Task 4 Step 4
- §4.3 路由 → Task 4 Step 1
- §5.1 启动入口 → Task 1 Step 3
- §5.2 启动校验 → Task 3 Step 1 (NOTEZ_SPACE_ROOT check)
- §5.3 重启 / 退出码 → Task 6 Step 1 (exit 5)
- §6.1 Dockerfile → Task 7 Step 1
- §6.2 使用说明 → Dockerfile + .dockerignore
- §7 CLI 清理 → Task 6
- §8.1 单元 → Task 3 Step 1 (4 tests in server module)
- §8.2 集成 → Task 5 (2 tests in v0_1_list_and_detail)
- §9 验收 → Task 8

**2. Placeholder scan:** 0 TBD/TODO/类似/同 Task.

**3. Type consistency:**
- `ResourceRow` defined in `app::model`, re-exported everywhere consistently.
- `#[server]` functions take `&Path` not `String` (impl takes &Path; server fn reads env var and converts).
- `use_server_future` used in both pages; the wrapper format is consistent.
- `Route::List {}` and `Route::Detail { encoded_ref: String }` match between router.rs and pages.

**4. Scope check:** 7 implementation tasks + 1 verification task. Each task is 1-2 hours of work. Matches the spec's v0.1 scope.

**Known risks:**
- Dioxus 0.6 EOL: `use_route::<Route>()` API might not match the exact form. Task 4 Step 1 has a NOTE allowing the engineer to adapt.
- Dioxus 0.6 `#[server]` macro requires the `dioxus = { features = ["fullstack"] }` feature which pulls in axum + tower 0.5; first `cargo build` will take longer.
- The `launch(app)` call assumes `app` is `pub fn app() -> Element`; Dioxus 0.6 sometimes wants `#[component] fn App`. Task 4 Step 5 wraps the actual `#[component]` inside.

---

**Plan complete and saved to `docs/superpowers/plans/2026-08-03-v01-web-client.md`.**
