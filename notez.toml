version = 2

[source]
name = "notez"
database = ".notez/index.sqlite"

[workflow]
todo = ["TODO", "NEXT", "WAIT"]
done = ["DONE", "QUIT"]

[[sources]]
id = "docs"
kind = "native"
path = "docs"
include_paths = ["docs"]
exclude_paths = [".notez"]
